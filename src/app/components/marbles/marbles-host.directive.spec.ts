import { MarblesHostDirective } from './marbles-host.directive';

describe('MarblesHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MarblesHostDirective();
    expect(directive).toBeTruthy();
  });
});
